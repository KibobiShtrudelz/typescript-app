import {
  Layout,
  // Avatar
} from "antd";
import React from "react";
// import { UserOutlined, LoginOutlined } from "@ant-design/icons";

const Header = () => {
  const { Header } = Layout;

  return <Header />;
};

export default Header;
